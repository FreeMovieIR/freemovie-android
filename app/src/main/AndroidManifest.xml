<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FreeMovie"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.FreeMovie">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- متغیرهای API -->
        <meta-data
            android:name="TMDB_BASE_URL"
            android:value="${TMDB_BASE_URL}" />
        <meta-data
            android:name="TMDB_API_KEY"
            android:value="${TMDB_API_KEY}" />
        <meta-data
            android:name="OMDB_BASE_URL"
            android:value="${OMDB_BASE_URL}" />
        <meta-data
            android:name="OMDB_API_KEY"
            android:value="${OMDB_API_KEY}" />

    </application>

</manifest>
